#version 400 core

in vec2 pass_texCoords;

out vec4 out_Color;

uniform sampler2D texSampler;
uniform float time;
uniform float pos;
void main(void){
	vec4 z = texture(texSampler,pass_texCoords);
	vec4 w = vec4(z.r,z.r,z.r,z.r);
	vec4 q = vec4(1,1,1,1);
	out_Color = vec4(0.0,pos,1.0, 1.0);
}